{
  "name": "vectracompute",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm install --prefix storefront --legacy-peer-deps && npm run build --prefix storefront",
    "dev": "npm run dev --prefix storefront",
    "start": "npm run start --prefix storefront"
  }
}
